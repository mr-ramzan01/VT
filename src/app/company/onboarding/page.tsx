"use client";
import OnboardingLayout from "../../../components/v1/onboarding/OnboardingLayout";

/**
 * Onboarding Page
 * 
 * This page contains the complete onboarding flow for the VTrade tokenization platform.
 * It uses the OnboardingLayout component which includes a multi-step process.
 */
export default function OnboardingPage() {
  return <OnboardingLayout />;
} 